; GEM file for electric field PA.
; Defines bottom and top electrode plates.

pa_define(201,401,1, planar, x,magnetic_,200, 0.5,0.5,0.5)  ; 0.5 mm/gu

e(0) {
  fill { within { box2d(0,0, 100,0) } }
}

e(20000) {
  fill { within { box2d(0,200, 100,200) } }
}
