; GEM file for electric field PA.
; Defines bottom and top electrode plates.

pa_define(201,201,1, planar, xy,magnetic_,, 0.5,0.5,0.5)  ; 0.5 mm/gu

e(100) {
  fill { within { box2d(0,0, 0,100) } }
}

e(0) {
  fill { within { box2d(100,0, 100,100) } }
}
