; GEM file for electric field PA.
; Defines left and right electrode plates.
; Generates plates_2dp.pa#.

pa_define(101,101,1, planar,none,electric,, 1,1,1)  ; 1 mm/gu

e(1) {
  fill { within { box2d(0,0, 0,100) } }
}

e(2) {
  fill { within { box2d(100,0, 100,100) } }
}
